# Copy to .env.local and fill in your Neon connection string
# (Neon console -> project wave-dash -> Connect -> pooled connection string)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/neondb?sslmode=require"
